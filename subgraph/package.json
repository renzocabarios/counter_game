{
  "name": "bayc",
  "license": "UNLICENSED",
  "scripts": {
    "prepare": "graph build && graph codegen ",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ counter_game",
    "create-local": "graph create --node http://localhost:8020/ counter_game",
    "remove-local": "graph remove --node http://localhost:8020/ counter_game",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 counter_game",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.94.0",
    "@graphprotocol/graph-ts": "0.36.0"
  },
  "devDependencies": {
    "matchstick-as": "0.6.0"
  }
}
